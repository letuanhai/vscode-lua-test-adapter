/**
 * The VS Code configuration namespace for this extension.
 *
 * This must match the key prefix used in the `contributes.configuration.properties`
 * section of package.json. Changing it here and doing a bulk rename of the prefix
 * in package.json is the only action needed to rename all configuration keys.
 */
export const CONFIG_NAMESPACE = "luaUnitTestAdapter";
